# ============================================================
# qcagent - Dependencies
# ============================================================

# Core
numpy>=1.24.0
pandas>=2.0.0
Pillow>=9.0.0
tqdm>=4.64.0
requests>=2.28.0

# PyTorch (install separately for CUDA compatibility)
# See: https://pytorch.org/get-started/locally/
torch>=2.0.0
torchvision>=0.15.0

# LLM API (Qwen via OpenAI-compatible HTTP endpoint, called with `requests`)
# No extra SDK needed; `requests` (listed under Core) is sufficient.

# WSI Processing
openslide-python>=1.3.0
h5py>=3.8.0

# Retrieval
faiss-cpu>=1.7.0
# Or: faiss-gpu>=1.7.0  (if GPU FAISS needed)

# Clustering (Patch Selection Tool-1)
scikit-learn>=1.2.0

# Vision-Language Models
transformers>=4.36.0

# Evaluation Metrics
# (no extra packages needed — BLEU/ROUGE/METEOR are implemented natively)

# Optional: BERTScore (requires GPU)
# bert-score>=0.3.13

# Optional: visualization for run_patho_r1_inference.py
# matplotlib>=3.6.0

# Optional: CONCH model
# timm>=0.9.0

# Optional: For running Patho-R1
# vllm>=0.4.0
